syntax = "proto3";

package calculator;

message SumRequest {
  repeated double number = 1;
}

message SumResponse {
  double result = 1;
}

service CalculatorService {
  rpc Sum (SumRequest) returns (SumResponse);
}
